<%= form_with(model: sample, local: true) do |form| %>
  <% if sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul>
      <% sample.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hoge %>
    <%= form.text_field :hoge, id: :sample_hoge %>
  </div>

  <div class="field">
    <%= form.label :fuga %>
    <%= form.text_field :fuga, id: :sample_fuga %>
  </div>

  <div class="field">
    <%= form.label :piyo %>
    <%= form.text_area :piyo, id: :sample_piyo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
